server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/merchant_management?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: devUserPassword123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

app:
  jwtSecret: 9a4f2c8d3b7a1e6f0h9g4d2b5m8k7i3j6
  jwtExpirationInMs: 86400000  # 24 hours

logging:
  level:
    org.springframework.security: DEBUG
    com.merchant: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
